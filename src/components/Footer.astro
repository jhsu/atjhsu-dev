<footer>
  <div class="footer-inner">
    <div class="footer-about">
      <p class="footer-name">Joe Hsu</p>
      <p class="footer-note">Notes on building software with AI — what works, what doesn’t, and the systems around the work.</p>
    </div>
    <nav class="footer-links" aria-label="Elsewhere">
      <a href="/blog">Writing</a>
      <a href="/about">About</a>
      <a href="/rss.xml">RSS</a>
      <a href="https://github.com/jhsu" target="_blank" rel="noopener">GitHub</a>
      <a href="https://x.com/jhsu" target="_blank" rel="noopener">X</a>
    </nav>
  </div>
  <p class="footer-colophon">Set in Merriweather and Fira Sans. Built with <a href="https://astro.build" target="_blank" rel="noopener">Astro</a>. © {new Date().getFullYear()} Joe Hsu.</p>
</footer>

<style>
  footer {
    border-top: 1px solid var(--surface-border);
    margin: clamp(4rem, 8vw, 6rem) auto 0;
    max-width: var(--page-width);
    padding: 2rem 1.5rem 2.5rem;
    width: 100%;
  }

  .footer-inner {
    display: flex;
    gap: 2rem;
    justify-content: space-between;
  }

  .footer-name {
    font-family: var(--font-family-serif);
    font-size: 1.05rem;
    font-weight: 700;
  }

  .footer-note {
    color: var(--text-secondary);
    font-family: var(--font-family-sans);
    font-size: 0.82rem;
    line-height: 1.6;
    margin-top: 0.4rem;
    max-width: 26rem;
  }

  .footer-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem 1.1rem;
    justify-content: flex-end;
    align-content: flex-start;
  }

  .footer-links a {
    color: var(--text-secondary);
    font-family: var(--font-family-sans);
    font-size: 0.82rem;
    text-decoration: none;
  }

  .footer-links a:hover {
    color: var(--accent-color);
  }

  .footer-colophon {
    border-top: 1px solid var(--surface-border);
    color: var(--text-tertiary);
    font-family: var(--font-family-sans);
    font-size: 0.72rem;
    margin-top: 1.75rem;
    padding-top: 1.25rem;
  }

  .footer-colophon a {
    color: inherit;
  }

  @media (max-width: 620px) {
    .footer-inner {
      flex-direction: column;
      gap: 1.5rem;
    }

    .footer-links {
      justify-content: flex-start;
    }
  }
</style>
