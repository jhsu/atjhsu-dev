<style>
  a {
    align-items: baseline;
    color: var(--text-primary);
    display: flex;
    flex-wrap: wrap;
    gap: 0.1rem 0.6rem;
    text-decoration: none;
  }

  .name {
    font-family: var(--font-family-serif);
    font-size: 1.35rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .descriptor {
    color: var(--text-tertiary);
    font-family: var(--font-family-sans);
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    line-height: 1.4;
    text-transform: uppercase;
  }

  a:hover .name {
    color: var(--accent-color);
  }

  @media (max-width: 520px) {
    .descriptor {
      display: none;
    }
  }
</style>

<a href="/" aria-label="Joe Hsu — home">
  <span class="name">Joe Hsu</span>
  <span class="descriptor">Writing &amp; notes</span>
</a>
