<aside aria-label="About the author">
  <img src="/assets/profile.png" alt="Joe Hsu" width="100" height="100" />
  <div>
    <p class="bio-name">Joe Hsu</p>
    <p>
      I’m a software engineer in New York. I write about building with AI agents and the systems around the work.
      <a href="/about">More about me <span aria-hidden="true">→</span></a>
    </p>
  </div>
</aside>

<style>
  aside {
    align-items: center;
    border-top: 1px solid var(--surface-border);
    display: grid;
    gap: 1.25rem;
    grid-template-columns: 3.5rem minmax(0, 1fr);
    margin: 0 auto;
    margin-top: 3rem;
    max-width: var(--reading-width);
    padding-top: 2rem;
  }

  img {
    background: var(--surface-muted);
    border-radius: 50%;
    filter: saturate(0.85);
    height: 3.5rem;
    object-fit: cover;
    width: 3.5rem;
  }

  .bio-name {
    color: var(--text-main);
    font-family: var(--font-family-serif);
    font-weight: 700;
    margin-bottom: 0.2rem;
  }

  p {
    color: var(--text-secondary);
    font-family: var(--font-family-sans);
    font-size: 0.88rem;
    line-height: 1.6;
    margin: 0;
  }

  a {
    color: var(--text-main);
    text-decoration: underline;
    text-decoration-color: color-mix(in srgb, var(--text-main) 30%, transparent);
  }

  a:hover {
    color: var(--accent-color);
    text-decoration-color: currentColor;
  }
</style>
